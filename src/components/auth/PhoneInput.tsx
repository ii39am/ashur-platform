import React from 'react';
import { phoneCountries, type PhoneCountryCode } from '../../config/onboarding';
export function PhoneInput({ country, national, language, labels, error, onChange }: { country: PhoneCountryCode; national: string; language: 'en' | 'ar'; labels: { phone: string; country: string; hint: string }; error?: string; onChange: (country: PhoneCountryCode, national: string) => void }) {
  const selected = phoneCountries.find((item) => item.code === country)!;
  return <div><label htmlFor="phone" className="mb-2 block text-sm font-semibold text-slate-200">{labels.phone}</label><div className="grid grid-cols-[minmax(9rem,auto)_1fr] gap-2" dir="ltr"><label className="sr-only" htmlFor="phoneCountry">{labels.country}</label><select id="phoneCountry" className="auth-input" value={country} onChange={(event) => onChange(event.target.value as PhoneCountryCode, national)}>{phoneCountries.map((item) => <option key={item.code} value={item.code}>{item.dial} {language === 'ar' ? item.ar : item.en}</option>)}</select><input id="phone" className="auth-input" inputMode="tel" autoComplete="tel-national" value={national} onChange={(event) => onChange(country, event.target.value)} aria-invalid={Boolean(error)} placeholder={selected.code === 'IQ' ? '770 123 4567' : ''} /></div><p className="mt-2 text-xs text-slate-500">{labels.hint}</p>{error && <p className="mt-2 text-sm text-rose-400">{error}</p>}</div>;
}
