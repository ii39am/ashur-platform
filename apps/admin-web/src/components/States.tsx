import React from'react';export function Loading(){return <div className="state" aria-busy="true"><span className="spinner"/>Loading…</div>}export function ErrorState({retry}:{retry?:()=>void}){return <div className="state error" role="alert">Administrative data is temporarily unavailable.{retry&&<button onClick={retry}>Try again</button>}</div>}export function Empty(){return <div className="state">No records match the current filters.</div>}export function Unavailable({title}:{title:string}){return <main className="page"><div className="page-title"><h1>{title}</h1></div><section className="state unavailable"><strong>Not configured</strong><p>This module has no approved authoritative data source and remains disabled.</p></section></main>}
