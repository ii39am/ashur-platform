import{useCallback,useEffect,useState}from'react';export function useApi<T>(loader:()=>Promise<T>,deps:unknown[]){const[data,setData]=useState<T|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState(false);const load=useCallback(async()=>{setLoading(true);setError(false);try{setData(await loader())}catch{setError(true)}finally{setLoading(false)}},deps);useEffect(()=>{void load()},[load]);return{data,loading,error,reload:load}}
