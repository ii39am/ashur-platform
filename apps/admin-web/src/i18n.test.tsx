import React from'react';import{render,screen}from'@testing-library/react';import userEvent from'@testing-library/user-event';import{beforeEach,describe,expect,it}from'vitest';import{LanguageProvider,useLanguage}from'./i18n';
function Demo(){const{language,setLanguage,t}=useLanguage();return <><span>{t.overview}</span><button onClick={()=>setLanguage(language==='en'?'ar':'en')}>switch</button><input defaultValue="filter-value"/></>}
describe('bilingual Admin UI',()=>{beforeEach(()=>localStorage.clear());it('switches RTL without losing form state',async()=>{render(<LanguageProvider><Demo/></LanguageProvider>);const input=screen.getByRole('textbox');await userEvent.type(input,'-kept');await userEvent.click(screen.getByRole('button'));expect(document.documentElement.dir).toBe('rtl');expect(input).toHaveValue('filter-value-kept');expect(screen.getByText('نظرة عامة')).toBeInTheDocument()});it('restores English LTR',()=>{render(<LanguageProvider><Demo/></LanguageProvider>);expect(document.documentElement.dir).toBe('ltr')})});
